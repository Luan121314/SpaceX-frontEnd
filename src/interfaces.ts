export interface NoticeProps {
    title: string;
    headline: string;
    notice: string
}

export interface UserProps {
    name: string;
    about: string;
    github: string;
    gender: string
}
